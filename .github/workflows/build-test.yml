on:
  push:
  workflow_call:
    
jobs:
  build:
    name: Build Project
    strategy:
      matrix: { dir: ['./javaumltranslator'], os: [ubuntu-latest, windows-latest] }
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: ${{ matrix.dir }}
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v4
        with:
          node-version: 20.15.0

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

  tests:
    name: Tests
    needs: build
    strategy:
      matrix: { os: [ubuntu-latest, windows-latest] }
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: ./javaumltranslator
    steps:
      - name: Test Project
        run: npm test