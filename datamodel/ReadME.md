# Datamodel

This project is a shared datamodel for every other projects that does parsing/source code generation.

## Installation

To install and use this project, you need to execute the following commands:

```sh
npm install

npm run build
```

The build is necessary for the other project to run and have typings in editor (Typescript).
